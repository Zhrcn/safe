'use client';

import { useState } from 'react';
import {
  Box,
  Typography,
  Paper,
} from '@mui/material';
import MedicineQuery from '@/components/doctor/MedicineQuery';
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from '@/components/ui/Card';

export default function MedicinePage() {
  return (
    <Card className="p-8 rounded-xl shadow-lg">
      <CardHeader>
        <CardTitle>Medicine Availability</CardTitle>
        <CardDescription>Check the availability of medicines at partner pharmacies and find alternatives when needed.</CardDescription>
      </CardHeader>
      <CardContent>
        <MedicineQuery />
      </CardContent>
    </Card>
  );
} 