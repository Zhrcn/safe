const express = require('express');
const router = express.Router();

// TODO: Import controller methods and middleware

// TODO: Define medicine routes (e.g., search, list)

module.exports = router;
